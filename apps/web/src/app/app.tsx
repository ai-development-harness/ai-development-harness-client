import './app.css';

export function App() {
  // Shell намеренно не обращается к local service и не задаёт transport-контракт.
  // ClientApi и пользовательские экраны добавляются после foundation в следующих STEP.
  return (
    <main>
      <h1>AI Development Harness Client</h1>
    </main>
  );
}
export default App;
