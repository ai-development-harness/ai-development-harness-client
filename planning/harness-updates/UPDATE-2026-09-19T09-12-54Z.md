# Обновление Harness до v0.4.0

## Контекст

- Начальный release: `v0.2.7`.
- Запрошенный и достигнутый release: `v0.4.0`.
- Проект остаётся неинициализированным: `project.initialized: false`.

## Применённый маршрут

1. `v0.2.7` → `v0.3.0` (`standard`, `reloadRequired: false`).
2. `v0.3.0` → `v0.4.0` (`standard`, `reloadRequired: true`).

После второго hop дальнейшее продолжение в текущем runtime не выполнялось: достигнут конечный target, а его `reloadRequired` зафиксирован как boundary.

## Изменения scope

- Добавлены `.project/command-transitions.json`, `docs/harness/COMMAND_SYNTAX.md`, `docs/harness/COMMAND_TRANSITIONS.md`, `docs/harness/EXECUTION_STATUS.md` и инструменты execution state/command validation в `tools/harness/`.
- Retired paths отсутствуют.
- Изменение ownership: `.project/command-transitions.json` добавлен в `harness_owned`.
- Локальные marker-блоки `README.md:PROJECT`, `AGENTS.md:PROJECT-CONTEXT` и `AGENTS.md:SKILL-ROUTING` сохранены.

## Проверки

- `python3 tools/harness/validate.py --mode manual` до mutation — PASS, 131 tracked files.
- Для обоих hop проверены immutable refs, policy transition, отсутствие коллизий новых managed paths и required artifacts target release.
- После mutation выполнена exact-сверка managed-файлов с `v0.4.0`, проверка required artifacts, marker-блоков и pre-init состояния — PASS.
- `python3 tools/harness/validate.py --mode commit` после mutation — PASS, 131 tracked files; предупреждение только об отсутствии staged files.
- `git diff --check` после mutation — PASS.
