# Harness update 2026-09-18T10:41:42Z

## Команда

```text
UPDATE HARNESS
```

## Release

- Initial release (lock BASE): `v0.2.6` (commit `2b53ff786729995ba158cfa40ab36e0bc0cc476e`)
- Resolved final target: `v0.2.7` (commit `1c69c254e0635bf69060fee20f164b29c5641b6a`)
- Source repository: `ai-development-harness/ai-development-harness-template`
- Routing metadata: remote `.project/harness-update-graph.json` из `main` (`schemaVersion: 1`, `latest: v0.2.7`)

Target взят из `latest` canonical remote graph, т.к. explicit `TO <tag>` не указан.

## Пройденный route

| # | from | to | kind | reloadRequired | статус |
|---|------|----|------|----------------|--------|
| 1 | `v0.2.6` | `v0.2.7` | `standard` | `false` | применён, postcondition PASS |

Route построен от `.project/harness.lock.json → source.ref`. Graph validation: `schemaVersion == 1`,
`latest` соответствует `tag_pattern`, каждый `from` имеет не более одного outgoing transition, cycles
отсутствуют, target достижим. Hop единственный, edge не пропускался, `reloadRequired` boundary на маршруте
нет, поэтому update завершён в один проход.

## Policy transition

- BASE `.project/harness-update.toml` (v0.2.6) == THEIRS (v0.2.7) == local OURS
  (sha256 `4083b6090d079e2f01e1114d4dd150a64a2f94834aabb0a3c5892a3dcf5ffee4`) — allowlist на этом hop не менялся.
- Transition scope: 90 конкретных managed paths (union BASE и THEIRS policy).
- Introduced managed paths: нет.
- Retired managed paths: нет.
- Ownership-reclassified paths: нет.
- `NEW_MANAGED_PATH_COLLISION`: 0.
- `OWNERSHIP_CLASS_CHANGE`: 0.
- Blockers до mutation: нет.

## Применённые изменения

### `harness_owned` (replace, условие `OURS == BASE` выполнено для каждого файла)

- `.agents/skills/reconcile-project/SKILL.md` — добавлен precondition `project.initialized: true` и
  контракт `RECONCILE PROJECT: NOT_APPLICABLE` с handoff → `INIT PROJECT`.
- `docs/harness/COMMANDS.md` — раздел `RECONCILE PROJECT` приведён к тому же precondition.
- `planning/EXECUTION_PROTOCOL.md` — раздел 16 дополнен pre-init guard для `RECONCILE PROJECT`.
- `.project/harness-update-graph.json` — добавлен transition `v0.2.6 → v0.2.7`, `latest: v0.2.7`.

Проверено: ни один `harness_owned` path не имел local modification относительно BASE, поэтому blocker
не сработал и replace был допустим.

### `shared` (3-way merge BASE/OURS/THEIRS)

- `.project/manifest.yaml` — `OURS == BASE`, конфликтов нет; изменилось только
  `harness.release: "0.2.6" → "0.2.7"`. `project.initialized` остался `false`.

### `marker_merge` (3-way merge + восстановление local generated blocks)

- `AGENTS.md` — upstream-содержимое между `v0.2.6` и `v0.2.7` не менялось, файл не трогался;
  блоки `PROJECT-CONTEXT` и `SKILL-ROUTING` сохранены.
- `README.md` — upstream-содержимое между `v0.2.6` и `v0.2.7` не менялось, файл не трогался.
  Local divergence находится внутри generated block `PROJECT` (строки 1–22) и сохранена как project-owned.

### Lock

- `.project/harness.lock.json`: `release: "0.2.6" → "0.2.7"`, `source.ref: "v0.2.6" → "v0.2.7"`,
  `updatedAt: 2026-09-18T10:41:42Z`. Lock продвинут только после успешного postcondition hop.

### Не затронуто

Project-owned / unknown paths не изменялись, в том числе `docs/requirements/SPEC.md`,
`docs/requirements/STATUS.md`, `docs/requirements/TEMPLATE.md`, `planning/tasks/TEMPLATE.md`, `docs/ui/**`.

`project.initialized` остался `false`. `INIT PROJECT` не выполнялся, product knowledge и STEP/REQ/ADR
не создавались.

## Verification evidence

Working tree до mutation: clean (`git status --porcelain` пуст), branch `main`.

Pre-mutation validator текущего Harness:

```text
Command: python3 tools/harness/validate.py --mode manual
Exit code: 0
Observed: HARNESS VALIDATION: PASS (130 tracked files checked, mode=manual)
```

Postcondition hop `v0.2.7` (проверка результата против immutable target tree и target
`.project/harness-policy.toml`, прочитанного как данные):

```text
Observed: managed paths OURS == THEIRS для всех 90 путей scope, кроме README.md (marker_merge, project block)
Observed: required_files=36 missing=0; required_skills=25 missing=0
Observed: POSTCONDITION: PASS
```

Post-mutation validator:

```text
Command: python3 tools/harness/validate.py --mode manual
Exit code: 0
Observed: HARNESS VALIDATION: PASS (130 tracked files checked, mode=manual)
```

Проверено: каждый применённый `harness_owned` path побайтово совпадает с `v0.2.7` (sha256 сверка);
`harness.release == "0.2.7"`; `project.initialized == false`; generated blocks `README.md`/`AGENTS.md` сохранены.

Target scripts, migration/install/bootstrap hooks из `v0.2.7` не запускались. `.project/harness-policy.toml`
и `tools/harness/validate.py` между `v0.2.6` и `v0.2.7` не менялись.

## Следующие шаги

```text
inspect diff
GIT CHECK
COMMIT
```
