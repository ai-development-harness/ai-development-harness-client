# Harness update 2026-09-18T10:02:34Z

## Команда

```text
UPDATE HARNESS TO v0.2.6
```

## Release

- Initial release (lock BASE): `v0.2.4` (commit `fd3d6410b5165e7a5e15fd559682f03e131b612d`)
- Requested final target: `v0.2.6` (commit `2b53ff786729995ba158cfa40ab36e0bc0cc476e`)
- Source repository: `ai-development-harness/ai-development-harness-template`
- Routing metadata: remote `.project/harness-update-graph.json` из `main` (`schemaVersion: 1`, `latest: v0.2.6`)

## Пройденный route

| # | from | to | kind | reloadRequired | статус |
|---|------|----|------|----------------|--------|
| 1 | `v0.2.4` | `v0.2.6` | `standard` | `false` | применён, postcondition PASS |

Route построен от `.project/harness.lock.json → source.ref`. Hop единственный, edge не пропускался,
`reloadRequired` boundary на маршруте отсутствует, поэтому update завершён в один проход.

## Policy transition

- BASE `.project/harness-update.toml` (v0.2.4) == THEIRS (v0.2.6) == local OURS — allowlist на этом hop не менялся.
- Transition scope: 90 конкретных managed paths (union BASE и THEIRS policy).
- Introduced managed paths: нет.
- Retired managed paths: нет.
- Ownership-reclassified paths: нет.
- `NEW_MANAGED_PATH_COLLISION`: 0.
- `OWNERSHIP_CLASS_CHANGE`: 0.
- Blockers до mutation: нет.

## Применённые изменения

### `harness_owned` (replace, условие `OURS == BASE` выполнено для каждого файла)

- `.agents/skills/documentation-sync/SKILL.md`
- `.agents/skills/fix-step/SKILL.md`
- `.agents/skills/implement-step/SKILL.md`
- `.agents/skills/init-project/SKILL.md`
- `.agents/skills/project-status/SKILL.md`
- `.project/harness-update-graph.json`
- `docs/harness/CI.md`
- `docs/harness/DOCUMENT_MODEL.md`
- `docs/harness/GETTING_STARTED.md`
- `docs/harness/GLOSSARY.md`
- `planning/EXECUTION_PROTOCOL.md`
- `tools/harness/validate.py`

### `shared` (3-way merge BASE/OURS/THEIRS)

- `.project/manifest.yaml` — `OURS == BASE`, конфликтов нет; изменилось только `harness.release: "0.2.4" → "0.2.6"`.

### `marker_merge` (3-way merge + восстановление local generated blocks)

- `AGENTS.md` — сохранены блоки `PROJECT-CONTEXT` и `SKILL-ROUTING`; приняты upstream-правки разделов 8 и 10.
- `README.md` — upstream-содержимое между `v0.2.4` и `v0.2.6` не менялось, файл не трогался.

### Lock

- `.project/harness.lock.json`: `release: "0.2.4" → "0.2.6"`, `source.ref: "v0.2.4" → "v0.2.6"`,
  `updatedAt: 2026-09-18T10:02:34Z`. Lock продвинут только после успешного postcondition hop.

### Не затронуто

Project-owned / unknown paths не изменялись, в том числе локально расходящиеся с upstream
`docs/requirements/SPEC.md`, `docs/requirements/STATUS.md`, `docs/requirements/TEMPLATE.md`,
`planning/tasks/TEMPLATE.md`.

`project.initialized` остался `false`. `INIT PROJECT` не выполнялся, product knowledge и STEP/REQ/ADR не создавались.

## Verification evidence

Pre-mutation validator текущего Harness:

```text
Command: python3 tools/harness/validate.py --mode manual
Exit code: 0
Observed: HARNESS VALIDATION: PASS (129 tracked/filesystem files checked, mode=manual)
```

Postcondition hop `v0.2.6` (read-only проверка результата против immutable target tree и target
`.project/harness-policy.toml`, прочитанного как данные):

```text
Observed: required_files=36 required_skills=25 required_agents=12
Observed: POSTCONDITION: PASS
```

Проверено: каждый managed `harness_owned` path побайтово совпадает с `v0.2.6`; все required files/skills/agents
присутствуют; `harness.release == "0.2.6"`; `project.initialized == false`; generated blocks `AGENTS.md` сохранены.

Target scripts, migration/install/bootstrap hooks из `v0.2.6` не запускались. Запуск обновлённого
`tools/harness/validate.py` относится к последующему `GIT CHECK`.

## Следующие шаги

```text
inspect diff
GIT CHECK
COMMIT
```
