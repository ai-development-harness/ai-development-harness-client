# UPDATE HARNESS — v0.1.2 → v0.2.4

- **Дата:** 2026-09-18T07:30:00Z
- **BASE (lock до update):** v0.1.2 (`d6480ee91d4967ecfa3bbf5c71c7fbe9112dc202`)
- **THEIRS (target):** v0.2.4 (`fd3d6410b5165e7a5e15fd559682f03e131b612d`), latest immutable tag на момент проверки
- **Source:** `ai-development-harness/ai-development-harness-template`

## Предыстория

Проект был на v0.1.1. Согласно `.project/harness-update-graph.json` target-релиза, переход `v0.1.1 → v0.1.2` — `bridge` (`reloadRequired: true`): updater v0.1.1 не содержит policy-transition semantics, поэтому прямой self-update был невозможен. Bridge был выполнен ранее через официальный one-shot `bootstrap-harness-v0.1.1-to-v0.1.2.sh` (без commit). Все последующие переходы графа (`v0.1.2 → v0.2.0 → v0.2.1 → v0.2.2 → v0.2.3 → v0.2.4`) — `standard`, поэтому применены одним `UPDATE HARNESS` от BASE v0.1.2 до latest v0.2.4.

## Policy transition

- Local `.project/harness-update.toml` и `.project/harness-policy.toml` до mutation побайтово совпадали с BASE (v0.1.2) — подтверждено сравнением git blob hash.
- `OWNERSHIP_CLASS_CHANGE`: не обнаружено.
- `NEW_MANAGED_PATH_COLLISION`: не обнаружено.
- Retired managed paths: нет.

## Introduced managed paths (16)

Claude Code runtime adapter (добавлен upstream в v0.2.0) + routing metadata (добавлено в v0.2.4):

- `.project/harness-update-graph.json` (harness_owned)
- `.claude/README.md` (harness_owned)
- `CLAUDE.md`, `.claude/settings.json`, `.claude/agents/**` (11 файлов) (shared)
- `docs/harness/CLAUDE_CODE.md` (harness_owned)

Ни один из этих путей не существовал локально ни в BASE, ни в OURS — созданы как новые.

## Retired / reclassified managed paths

Нет.

## Изменённый существующий managed-контент (18 файлов)

Для каждого подтверждено `OURS == BASE` (git blob hash) до mutation, поэтому 3-way merge → результат = THEIRS без конфликтов:

`.agents/skills/update-harness/SKILL.md`, `.codex/agents/harness-updater.toml`, `.gitignore`, `.project/README.md`, `.project/harness-policy.toml`, `.project/harness-update.toml`, `.project/manifest.yaml`, `docs/harness/{AGENT_CONFIGURATION,COMMANDS,GETTING_STARTED,MAINTENANCE,README,REPOSITORY_LAYOUT,UPDATES}.md`, `planning/harness-updates/README.md`, `tools/harness/validate.py`.

`marker_merge` (3-way merge вне generated blocks + восстановление local blocks):

- `AGENTS.md` — блоки `PROJECT-CONTEXT`, `SKILL-ROUTING` сохранены дословно из OURS.
- `README.md` — блок `PROJECT` сохранён дословно из OURS.

## Verification evidence

- `python3 tools/harness/validate.py --mode manual` до mutation: `PASS (111 tracked/filesystem files checked, mode=manual)`.
- Каждый применённый target-файл сверен по git blob SHA с immutable tag `v0.2.4` (35/35 совпадений).
- Все `required_files` (36) и `required_skills` (25) из обновлённого `.project/harness-policy.toml` присутствуют в working tree после mutation.
- `.project/harness.lock.json` обновлён на `release: "0.2.4"`, `source.ref: "v0.2.4"`.

## Не сделано автоматически

- Не запускались target scripts/hooks/install commands.
- Не создавались STEP/REQ/ADR.
- Не выполнялись commit/push/PR.
- Untracked `bootstrap-harness-v0.1.1-to-v0.1.2.sh` (артефакт bridge-этапа) не тронут — оставлен на усмотрение пользователя.

## Следующий шаг

`GIT CHECK` → `COMMIT`.
